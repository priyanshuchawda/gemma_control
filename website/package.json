{
  "name": "gemmacontrol-download-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static TypeScript download website for GemmaControl.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run clean && tsc -p tsconfig.build.json && node scripts/copy-static.mjs && node scripts/verify-build.mjs",
    "verify": "npm run typecheck && npm run build",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
